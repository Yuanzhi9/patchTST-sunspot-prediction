# EXP-20-3 — 高值加权 MSE（探索期，Stage 9）

> 探索期队列第三实验。判定标准待议——只收集数据，不下有效/无效结论。

| 字段 | 内容 |
|------|------|
| 日期 | 2026-08-15 |
| 实验ID | EXP-20-3 |
| 目的 | 打峰值压制机制1（MSE账本偏袒低值）：loss 改为高值加权 MSE，抬高高活动水平样本权重 |
| 改动变量 | loss（唯一变量）：mse(基线) → wmse（权重=1+clamp(z_true,min=0)） |
| 对照基线 | EXP-18-5b（loss=mse, W1：全步=22.62, R²=0.789, E_r=-24.4, 滚动MAE=50.41, 滚动峰=217.6/244.3） |
| 代码改动 | exp_main.py 新增 WeightedMSELoss 类 + `--loss wmse` 分支（SOP §4-B 范围内，已备份+注释标注） |
| 训练入口 | run_longExp.py（命令链模式） |
| 口径 | 最佳 val 模型 |
| 豁免标注 | 本队列一次性豁免 SOP §8（仅此一次） |

## 可证伪假设

MSE 下高值月份占比少、loss 由低值主导，模型最优策略是压低峰值。加权后（峰值样本权重最高 ~4.5x），模型应更愿意顶到峰值。

- 若机制1成立：E_r（峰值误差）和滚动峰值误差应显著改善
- 代价预期：低值区误差可能上升，全步 MAE 可能变差
- 注意：wmse 的 val loss 数值与 mse 不可比（权重不同），EarlyStopping 最佳点选择在各自口径下独立进行

## 参数对照表

| 参数 | 基线(5b) | EXP-20-3 | 一致？ |
|------|---------|----------|--------|
| features/窗口/seq_len/pred_len | MS/W1/336/24 | 同 | ✓ |
| d_model/n_heads/e_layers/d_ff | 128/8/2/2048 | 同 | ✓ |
| patch_len/stride/dropout/fc_dropout | 16/8/0.2/0.2 | 同 | ✓ |
| revin/affine/individual/activation | 1/0/0/gelu | 同 | ✓ |
| batch_size/lr/epochs/patience/seed | 16/1e-4/50/100/2021 | 同 | ✓ |
| **loss** | **mse** | **wmse** | ✗ 唯一变量 |

## 结果（跑后填）

| 指标 | 基线 loss=mse (5b) | EXP-20-3 (wmse) |
|------|:---:|:---:|
| 全步 MAE | 22.62 | 25.92 |
| step0 | 11.01 | 10.09 |
| R² | 0.789 | 0.730 |
| E_r | -24.4 | +2.6 |
| 滚动 MAE | 50.41 | 59.79 |
| 滚动峰值(预测/真实) | 217.6/244.3 | 242.1/244.3 (-2.2) |
| 150+ 分层 | 28.4 | 27.3 |
| 0-50 分层 | 12.3 | 15.8 |

## 结论（判定待议，跑后收集数据）

---

## SOP 预检表缺项补写（2026-08-15 事后补写，只作留档）

> 本实验跑前未完整填写 SOP §1 预检表。以下内容为事后依据当时的方案讨论回填，
> 不构成'跑前已完成预检'的证明。2026-08-15 起新实验执行跑前门禁：预检表不写完不 launch。
### 预尸检（补写）

最可能：峰值顶起来但低值区损伤（全步恶化）；峰值样本占比少（SSN>150 约 10-15%），加权可能过拟合少数样本。

### 文献先例（补写）

高值加权 loss 出自 A-H 清单 D1（2026-05-07 提出）；Huber 已在 EXP-9 验证无效（MS 下与 MSE 相同）。

### 结论前提条件（补写）

结论仅在 features=MS、W1、最佳模型口径、50ep、单种子、sl336+pl16s8+el2+df2048+dm128+do0.2、权重函数 w=1+clamp(z,0) 下成立。

### 成功标准（补写）

峰值改善（E_r/滚动峰误差）与全步恶化同时报告；判定条款属待议项③，当时未定稿。
