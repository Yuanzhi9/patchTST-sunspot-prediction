# EXP-18 Round 5 — dropout 搜索 (W1窗口: Cycle 23)

> SOP §1 预检表（2026-08-14 跑前填写）→ 结果和结论跑后补。

| 字段 | 内容 |
|------|------|
| 日期 | 2026-08-14 |
| 实验ID | EXP-18-5a (do=0.1) / EXP-18-5b (do=0.2) |
| 目的 | 在定案基线(sl336, pl16/s8, el=2, df=2048, dm=128)上，测试 dropout 0.05→0.1/0.2 是否改善 |
| 改动变量 | dropout + fc_dropout（绑定）：(0.05,0.05)基线 → (0.1,0.1) → (0.2,0.2)。head_dropout 保持 0.0 |
| 对照基线 | EXP-17-0b 补测（do=0.05，全步MAE=23.87, R²=0.769） |
| 训练入口 | run_longExp.py（命令链模式） |
| features | MS |
| 口径 | 最佳 val 模型 |

---

## 可证伪假设

2838 样本 + sl336 + 50ep 下基线已见过拟合迹象（最佳点 epoch 4）。更强的 dropout 是直接的正则化手段。若 0.05 正则不足，0.1 应改善测试集；若 0.1/0.2 压制过头，训练 loss 将不降或测试恶化。

- 如果 do=0.1 全步 MAE 改善 ≥3%：正则不足，选 0.1（0.2 若更好则选 0.2）
- 如果 0.1/0.2 与基线差 <3%：dropout 不敏感，维持 0.05
- 如果 0.2 恶化 ≥5%：过正则化，选 0.1（若 0.1 也恶化则维持 0.05）

## 预尸检

1. 最可能：0.1/0.2 与 0.05 差异 <3%——最佳模型口径已经通过选 epoch 规避了部分过拟合，dropout 的边际作用变小
2. 特殊风险：do=0.2 在 50ep 下训练 loss 可能长期不降（学不动）。**监控规则：若训练段日志显示 epoch 15 时 train loss > 0.5（基线 epoch 1 约 0.75、epoch 5 约 0.1），判定学不动，中止该实验并记录**——不硬等 50ep 浪费时间
3. 绑定搜索的归因问题：若 0.1 有改善，无法区分 encoder dropout 与 fc_dropout 的贡献。若改善 ≥8% 才值得拆开验证（本轮预期改善小，不做）

## 参数对照表

| 参数 | 基线(0b) | 5a | 5b | 一致？ |
|------|---------|-----|-----|--------|
| features/窗口/seq_len/pred_len | MS/W1/336/24 | 同 | 同 | ✓ |
| d_model/n_heads/e_layers/d_ff | 128/8/2/2048 | 同 | 同 | ✓ |
| patch_len/stride | 16/8 | 同 | 同 | ✓ |
| **dropout/fc_dropout** | **0.05/0.05** | **0.1/0.1** | **0.2/0.2** | ✗ 唯一变量 |
| head_dropout | 0.0 | 0.0 | 0.0 | ✓ |
| revin/individual/activation | 1/0/gelu | 同 | 同 | ✓ |
| batch_size/lr/loss/epochs/patience/seed | 16/1e-4/mse/50/100/2021 | 同 | 同 | ✓ |

## 结论前提条件

features=MS、W1、最佳模型口径、50ep、单种子、sl336+pl16/s8+el=2+df=2048+dm=128 下成立。
（⚠️ 单种子弊端与解决想法见 data_pipeline.md 顶部警示：无重复标准差、3%边缘差异不可靠；想法=多种子重跑+bootstrap，不写死）

## 成功标准

| 结果 | 判定 | 动作 |
|------|------|------|
| 0.1 改善 ≥3% | 正则不足 | 选 0.1 或 0.2（取更优） |
| 两者均 <3% 差异 | 不敏感 | 维持 0.05 |
| 0.2 恶化 ≥5% 且 0.1 正常 | 过正则 | 选 0.1 |
| 训练学不动（见预尸检规则） | 无效 | 中止 + 记录 |

---

## 结果（跑后填）

| 指标 | 基线 do=0.05 | EXP-18-5a (0.1) | EXP-18-5b (0.2) |
|------|:---:|:---:|:---:|
| 全步 MAE | 23.87 | 22.92 | **22.62** |
| step0 | 11.39 | **10.56** | 11.01 |
| R² | 0.769 | 0.785 | **0.789** |
| E_r | **-17.8** | -20.0 | -24.4 |
| 150+ 分层 | 29.0 | 29.1 | **28.4** |
| 相对基线 | — | +4.0% | +5.2% |

## 结论（跑后填）

**✅ 通过 — 更新基线为 dropout=0.2/fc_dropout=0.2。阶段1首个实质改善。**

1. 回答了什么：dropout 是正则敏感参数，0.05 正则不足。0.1 改善 4.0%，0.2 改善 5.2%（全步 22.62，最优）
2. 预检表"0.2 学不动"风险未发生：训练正常（val loss 0.60→0.27 持续下降），无过正则化迹象
3. ⚠️ E_r 张力（重要观察）：do 越大峰值幅度误差越负（-17.8 → -20.0 → -24.4），0.2 全步最优但峰值低估最深。全步/R² 主指标支持 0.2；峰值误差恶化留档，验证阶段重点观察
4. 绑定搜索限制：无法区分 encoder dropout 与 fc_dropout 贡献。改善 5.2% 未达 8% 拆分阈值，维持绑定
5. 下一步：基线更新为 do=0.2。Round 6（activation）用新基线

**收尾判定**：✅ 通过 — 结论进入知识库。

## 自检

- [x] 只改 1 个变量（dropout+fc_dropout 绑定）
- [x] 命令链模式
- [x] 物理+z-score 双报
- [x] result.txt 追加
- [x] 落盘 commit + push

---

## SOP 预检表缺项补写（2026-08-19 事后补写，只作留档）

> 本实验跑前未完整填写 SOP §1 预检表。以下内容为事后依据当时的方案讨论回填，
> 不构成'跑前已完成预检'的证明。2026-08-19 起新实验执行跑前门禁：预检表不写完不 launch。
### 文献先例（补写）

PatchTST 原文建议值与本研究基线一致；本维度（d_model/dropout/activation/individual）无太阳黑子专有文献先例，标注"本项目内首次干净对照"。
