# EXP-18 Round 3 — d_ff 搜索 (W1窗口: Cycle 23)

> SOP §1 预检表（2026-08-14 跑前填写）→ 结果和结论跑后补。

| 字段 | 内容 |
|------|------|
| 日期 | 2026-08-14 |
| 实验ID | EXP-18-3a (df=512) / EXP-18-3b (df=1024) |
| 目的 | 在定案基线(sl336, pl16/s8, el=2)上，测试 d_ff 2048→512/1024 是否影响全步 MAE |
| 改动变量 | d_ff（唯一变量）：2048(基线) → 512 → 1024 |
| 对照基线 | EXP-17-0b 补测（df=2048，全步MAE=23.87, step0=11.39, R²=0.769, E_r=-17.8） |
| 训练入口 | run_longExp.py（命令链模式：train && 补测，见 SOP §4） |
| features | MS |
| 口径 | 最佳 val 模型（EarlyStopping full_checkpoint.pth） |

---

## 可证伪假设

d_ff 是 FFN 中间维度，d_ff=2048 下 FFN 参数 ≈ 128×2048×2×2层 ≈ 1M。历史从未有干净对照（EXP-14 用 2048，EXP-16c 用 256，混了其他变量）。若 FFN 宽度不是瓶颈，缩小到 512（参数 1/4）应无差异；若 2048 确实是容量必需，缩小会恶化。

- 如果 df=512 或 1024 全步 MAE 与基线差距 <3%：FFN 宽度非瓶颈 → 选最小 512（最便宜）
- 如果 df=512 恶化 ≥5% 而 1024 不恶化：容量瓶颈在 512-1024 之间 → 选 1024
- 如果都恶化 ≥5%：2048 是必需容量 → 维持基线

## 预尸检

1. 最可能结果：三者 <3% 无差异——d_ff 2048 对 d_model=128 的模型是过度配置，512 足够。若如此，本轮白赚一个便宜基线
2. 风险：df 变小后 E_r（峰值误差）可能恶化——FFN 容量与峰值拟合能力有关。若全步无差异但 E_r 显著恶化（>10 绝对值），不能只看全步 MAE，需汇报用户
3. 单种子：df=512 和 2048 差距若在 3% 边缘（2.5-3.5%），判定存疑
（⚠️ 单种子弊端与解决想法见 data_pipeline.md 顶部警示：无重复标准差、3%边缘差异不可靠；想法=多种子重跑+bootstrap，不写死）

## 文献先例

PatchTST 原文 d_ff=2048（d_model=128 时）。无太阳黑子专有先例。本项目内首次干净对照。

## 参数对照表

| 参数 | 基线(0b) | 3a | 3b | 一致？ |
|------|---------|-----|-----|--------|
| features/窗口/seq_len/pred_len | MS/W1/336/24 | 同 | 同 | ✓ |
| d_model/n_heads/e_layers | 128/8/2 | 同 | 同 | ✓ |
| **d_ff** | **2048** | **512** | **1024** | ✗ 唯一变量 |
| patch_len/stride | 16/8 | 同 | 同 | ✓ |
| revin/dropout/individual/activation | 1/0.05/0/gelu | 同 | 同 | ✓ |
| batch_size/lr/loss | 16/0.0001/mse | 同 | 同 | ✓ |
| train_epochs/patience/seed | 50/100/2021 | 同 | 同 | ✓ |

## 结论前提条件

结论仅在：features=MS、W1 回测窗口、最佳模型口径、50ep、单种子、sl336+pl16/s8+el=2 下成立。

## 成功标准

| 结果 | 判定 | 动作 |
|------|------|------|
| 两者均 <3% 差异 | FFN 宽度非瓶颈 | 选 df=512（最便宜） |
| 512 恶化≥5% 且 1024 正常 | 瓶颈在 512-1024 间 | 选 df=1024 |
| 都恶化 ≥5% | 2048 必需 | 维持基线 |
| E_r 显著恶化（即使全步无差异） | 存疑 | 停下汇报用户 |

---

## 结果（跑后填）

| 指标 | 基线 df=2048 | EXP-18-3a (df=512) | EXP-18-3b (df=1024) |
|------|:---:|:---:|:---:|
| 全步 MAE | **23.87** | 25.32 | 24.83 |
| step0 | 11.39 | 12.67 | 14.97 |
| R² | **0.769** | 0.741 | 0.752 |
| E_r | -17.8 | +3.8 | -4.7 |
| 150+ 分层 | 29.0 | 29.0 | **28.4** |
| 相对基线 | — | +6.1% | +4.0% |

## 结论（跑后填）

**✅ 通过 — 维持 d_ff=2048，维度关停。**

1. 回答了什么：d_ff 是单调敏感参数——512 恶化 6.1%，1024 恶化 4.0%，2048 最优。FFN 容量确实影响该任务的性能，2048 是既有候选中最优
2. 判定依据：512 恶化 ≥5%（容量明显不足）；1024 恶化 4.0% 处于 3-5% 灰色带，但三数据点趋势单调（512<1024<2048），且 2048 在全部指标（全步/R²/step0）上最优 → 维持基线
3. 预检表"E_r 显著恶化"条件未触发：512 的 E_r=+3.8（转高估）与 1024 的 -4.7 波动大，但全步主指标已足以决策
4. 备注：未测试 df=4096（计划外，用户限制搜索规模）。2048 最优仅限 {512,1024,2048} 内，方向单调暗示更大 df 可能继续改善——若阶段1收尾时预算允许，可作为追加实验提议，不在此轮扩展
5. 下一步：d_ff 固定 2048。Round 4（d_model 64/128/256）基线不变

**收尾判定**：✅ 通过 — 假设完整验证，结论进入知识库。

## 自检

- [x] 只改 1 个变量（d_ff）
- [x] 对照基线同配置
- [x] 命令链模式
- [x] 物理+z-score 双报
- [x] result.txt 追加
- [x] 落盘 commit + push
