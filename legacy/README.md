# legacy/ — 待清理文件

本目录内的文件不再使用，保留仅供历史参考。不主动删除但也不会在文档中引用。

| 文件 | 为什么在这里 |
|------|-------------|
| `plot001_pred.py` | 空文件（0 字节），无内容 |
| `import numpy as np.py` | 文件名是误操作产生的（import 语句），内容为 npy 查看脚本 |
| `run-longexp.txt` | run_longExp.py 的完整副本，被误存为 .txt 扩展名 |
| `run_longExp.py.bak / .bak.20260404_1018` | 两个历史备份版本 |
| `start_test.bat` | Windows 批处理，本项目在 Linux 上运行 |
| `pred_manual.npy / pred_original.npy` | 早期推理测试产生的 npy 文件，无对应配置记录 |
| `final_water_vapor_flux.png` | 水汽通量图——与太阳黑子无关，可能是其他项目混入 |
| `analyse-jixiantest001.ipynb` | Jupyter notebook，与 `PatchTST_supervised/` 内重复，此副本为根目录冗余 |
