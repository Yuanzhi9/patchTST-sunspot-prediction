# EXP-20-1 — RevIN=0 诊断（探索期，Stage 9）

> 探索期队列第一实验。判定标准为待议项——本实验只收集数据，不下有效/无效结论。

| 字段 | 内容 |
|------|------|
| 日期 | 2026-08-14 |
| 实验ID | EXP-20-1 |
| 目的 | 诊断峰值压制机制3：RevIN 逐窗口反变换是否锚定压低峰值。关掉 RevIN（revin=0），看峰值误差是否改善 |
| 改动变量 | revin（唯一变量）：1(基线) → 0 |
| 对照基线 | EXP-18-5b（W1：全步=22.62, step0=11.01, R²=0.789, E_r=-24.4, 滚动MAE=50.41, 滚动峰=217.6/244.3） |
| 训练入口 | run_longExp.py（命令链模式） |
| features | MS |
| 口径 | 最佳 val 模型 |
| 豁免标注 | 本队列（EXP-20-1~4）为预先批准的整体队列，一次性豁免 SOP §8 停止规则（仅此一次） |

---

## 可证伪假设

revin=1 时，模型输入被逐窗口去均值除方差，输出反变换时乘回**输入窗口**的均值方差。上升期窗口均值落后于未来峰值 → 预测峰顶被锚定压低（机制3）。revin=0 时模型直接看 StandardScaler 全局 z 值，无窗口锚定。

- 如果机制3成立：revin=0 的 E_r（峰值幅度误差）和滚动峰值误差应显著改善
- 代价预期：全步 MAE 可能变差（窗口归一化消失，长程趋势主导，训练难度上升）

**判定（待议，先收集数据）**：全步 MAE 为主；若 E_r 或滚动峰值误差改善 ≥20% 且全步恶化 <5%，拟判"峰值有效"（此条款待用户确认后生效）。

## 预尸检

1. 最可能：全步变差 + 峰值改善——机制3成立，但要付出全步代价
2. 次可能：全面变差——revin=0 下 50ep 训练不收敛（无窗口归一化，z 值范围 -1.5~3.5 全程摊开，训练更难）。若训练段 val loss 不降（epoch 15 仍 >0.5），按预尸检规则中止并记录
3. 历史对照：Stage 0 是 MinMax+revin=0（差，不可迁移）；StandardScaler+revin=0 是全新组合，无先例

## 文献先例

PatchTST 原文默认 RevIN=1；EXP-14 及阶段1全部 revin=1。无太阳黑子专有先例，标注"本项目内首次"。

## 参数对照表

| 参数 | 基线(5b) | EXP-20-1 | 一致？ |
|------|---------|----------|--------|
| features/窗口/seq_len/pred_len | MS/W1/336/24 | 同 | ✓ |
| d_model/n_heads/e_layers/d_ff | 128/8/2/2048 | 同 | ✓ |
| patch_len/stride | 16/8 | 同 | ✓ |
| dropout/fc_dropout/head_dropout | 0.2/0.2/0.0 | 同 | ✓ |
| individual/activation | 0/gelu | 同 | ✓ |
| **revin** | **1** | **0** | ✗ 唯一变量 |
| affine | 0 | 0 | ✓ |
| batch_size/lr/loss/epochs/patience/seed | 16/1e-4/mse/50/100/2021 | 同 | ✓ |

## 结论前提条件

features=MS、W1 回测窗口、最佳模型口径、50ep、单种子、sl336+pl16/s8+el2+df2048+dm128+do0.2 下成立。
（⚠️ 单种子弊端与解决想法见 data_pipeline.md 顶部警示：无重复标准差、3%边缘差异不可靠；想法=多种子重跑+bootstrap，不写死）

## 成功标准（待议，先收集数据）

全步 MAE 与 E_r/滚动峰值误差双报告；判定条款待用户确认。

---

## 结果（跑后填）

| 指标 | 基线 revin=1 (5b) | EXP-20-1 (revin=0) |
|------|:---:|:---:|
| 全步 MAE | 22.62 | 22.72 |
| step0 | 11.01 | 14.99 |
| R² | 0.789 | 0.791 |
| E_r | -24.4 | -27.5 |
| E_m | 3 | 1 |
| 滚动 MAE | 50.41 | 19674212.76 (崩溃) |
| 滚动峰值(预测/真实) | 217.6/244.3 | -1601.1/244.3 (负数!) |
| 150+ 分层 | 28.4 | 29.1 |

## 结论（判定待议，跑后收集数据）

## 数据收集完毕（判定待议）

| 指标 | 基线 revin=1 (5b) | EXP-20-1 (revin=0) | 观察 |
|------|:---:|:---:|------|
| 全步 MAE | 22.62 | 22.72 | 无差异（+0.4%） |
| step0 | 11.01 | 14.99 | 恶化 36% |
| R² | 0.789 | 0.791 | 相同 |
| E_r | -24.4 | -27.5 | 峰值误差更差 |
| 滚动 MAE | 50.41 | **19674212.76** | **彻底崩溃** |
| 滚动峰值 | 217.6/244.3 | **-1601.1/244.3** | **物理不可能的负数** |

**关键科学发现**：revin=0 在全步口径下与 revin=1 无差异，但滚动口径彻底崩溃（后期/前期 110827x，误差自回归指数放大）。结论：**RevIN 不是峰值压制的元凶，反而是滚动稳定性的支柱**——窗口归一化在滚动回填时充当"误差抑制器"，每次重置数据范围，阻止误差累积。机制3假设在 W1 滚动口径下被证伪；峰值压制归因回到机制1（MSE账本）和机制2（Scaler锚点）。

revin=0 方向可关停（滚动崩溃 + 全步无差异 + E_r 未改善）。正式判定待判定标准定稿。
