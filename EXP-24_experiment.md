# EXP-24 — 极小值专项评估（阶段3 前置，分析任务）

| 字段 | 内容 |
|------|------|
| 日期 | 2026-08-20 |
| 实验ID | EXP-24 |
| 类型 | 分析任务（复用三窗口已定论 checkpoint，无训练） |
| 目的 | 回答"当前最优配置在谷值区的时间误差和幅度误差各是多少"——为阶段3 的"25 周期最小年+最小值"目标提供误差预期；验证"接缝弱区"（W2 step0=36）是否系统性 |
| 数据口径 | **月均 SSN 口径为主**（模型天然口径）；13 月平滑作参照列（⚠️ 平滑谷受边界效应污染，见 data_pipeline.md 警示） |
| 对照 | 对照=真实观测值；模型=阶段1 已定论配置（EXP-18-5b / EXP-19-2 / EXP-19-3），**不涉及待批注的探索期模型** |

---

## 可证伪假设（三行）

- 如果谷值时间误差多数 ≥6 个月 → 阶段3 谷值时间预测不可直接外推，需专门策略（block 谷前切入等）
- 如果谷值时间误差多数 ≤3 个月 → 常规流程可行
- 如果 W1 终点谷（2008-08）能被 1996 起点发起的轨迹预测准（时间误差 ≤6 月）→ 全周期外推谷值可信

## 预尸检（如果两周后证明这分析是废物，最可能因为什么）

1. **结构差异**（实测修正版）：W1 有完整"谷(1996-10)→峰→谷(2008-08)"结构，样本质量最好；W2 终点、W3 终点只有谷前段——结论按窗口分别标注，不混合
2. **预测侧"无谷"风险**：滚动/block 轨迹在相应段可能无最小值（单调上升）→ 时间误差无法计算。处置：记"无谷"，只报幅度误差，不得伪装成"时间误差=0"
3. **样本量**：5 个谷点，统计效力弱——只做个案描述+定性，不做统计外推
4. **指标敏感度**：谷值绝对 SSN 极小（0.2-3.0），绝对幅度误差必然"好看"——**相对误差和时间误差才是敏感指标**，报告按敏感度排序（时间 > 相对幅度 > 绝对幅度）
5. **对拍失败**：复用脚本参数（num_train/窗口）抄错导致 MAE 对不上已记值——对拍门禁兜底

## 文献先例

文献中极小值预测普遍弱于峰值（谷值 SSN 低、信噪比差）；无谷值专项先例，本项目内首次。

## 坑清单回顾（必填行）

本实验可能踩的坑编号：**26**（口径分裂——三口径分栏报告）；**24**（复用已验证函数，不重写新逻辑）；**25**（本任务模型均无变换，target_transform 一律为空，标注排除依据）。

## 参数对照表（数据来源）

| 项 | W1 | W2 | W3 |
|----|-----|-----|-----|
| checkpoint | EXP-18-5b | EXP-19-2 | EXP-19-3 |
| num_train | 2838 | 2986 | 3118 |
| n_roll | 148 | 132 | 71 |
| 轨迹 | 滚动/block24无重叠（复用已验证脚本） | 同 | 同 |

## 结论前提条件

结论基于月均口径、阶段1 已定论配置、单种子；谷值窗口=谷点后侧 12 月（谷点前侧在测试段外，无法评估）；平滑口径谷值受边界效应污染，仅作参照。
（⚠️ 单种子弊端与解决想法见 data_pipeline.md 顶部警示：无重复标准差、3%边缘差异不可靠；想法=多种子重跑+bootstrap，不写死）

## 成功标准

产出三窗口 5 谷点的三口径误差表 + "接缝弱区是否系统性"的答案 + 结构局限标注——数据完整、对拍通过即达成；不下"有效/无效"结论（这是评估任务）。

---

## 结果（跑后填）

## 结果（2026-08-20 完成，对拍全 PASS）

对拍：三窗口滚动/block MAE 与已记值逐位一致（50.41/21.29 等）。

### 谷点三口径评估表（月均口径）

| 谷点 | 段 | 真实谷 | 滚动：时间/绝对差 | 滚动：预测谷值 | block：时间/绝对差 | block：预测谷值 |
|------|-----|--------|------------------|----------------|-------------------|----------------|
| W1 1996-10 | 后侧12月 | 0.7 | +1月 / -6.9 | **-6.2 ⚠️负值** | 无谷 / +3.5 | 4.2（段内最小） |
| W1 2008-08 | 完整±12月 | 0.3 | **无谷 / -194.5（崩塌）** | **-194.2 ⚠️负值** | -3月 / +15.0 | 15.3 |
| W2 2009-08 | 后侧12月 | 0.0 | 无谷 / +55.6 | 55.6（段内最小） | +1月 / +31.5 | 31.5 |
| W3 2020-02 | 后侧12月 | 0.2 | +9月 / -7.0 | **-6.8 ⚠️负值** | +2月 / -2.6 | **-2.4 ⚠️负值** |

平滑参照（真实序列 13 月平滑）：谷值 13.3/3.2/12.3/3.4——⚠️ 边界效应污染，仅作参照。

### 假设验证

- "谷值时间误差 ≥6 月 → 阶段3 不可直接外推"：**滚动证实**（W3 +9 月、W1/W2 无谷或崩塌）；block 时间误差 -3/+1/+2 月相对可用
- "W1 终点谷（2008-08）能被预测准"：**滚动崩塌（-194.2），block 部分可用（时间 -3 月、幅度 +15）**
- 幅度预测：**全部崩塌**——真实谷 0.0-0.7，预测从 -194 到 55.6，量级级错误

### 结论（定性，样本少不统计外推）

1. **"接缝弱区"被证实系统性**：谷值区是模型最弱区——滚动多数无谷或负值崩塌，block 时间误差可用（±3月）但幅度偏差 1-2 个量级
2. **谷值幅度预测当前不可靠**（无论月均还是平滑口径）——阶段3 "25 周期最小值"预测需专门策略（谷值区 loss 加权、平滑口径预测等）或承认大误差
3. **block 在谷值时间定位上系统性优于滚动**（-3/+1/+2 vs +1/无谷/+9）——与 block 优于滚动的全窗口结论一致
4. **负值崩塌是普遍现象**：模型 z 空间下探超出 scaler 范围——评估/外推时应 clip 到 ≥0（物理约束），但 clip 不解决预测质量
5. ⚠️ 局限：谷点前侧 12 月在测试段外（除 W1 终点谷）；平滑谷受边界效应污染；单种子

### 收尾判定

✅ 评估任务达成：三口径数据完整、对拍通过、结论定性呈现。不下有效/无效判定（本任务为阶段3 前置评估）。
