# EXP-18 Round 4 — d_model 搜索 (W1窗口: Cycle 23)

> SOP §1 预检表（2026-08-14 跑前填写）→ 结果和结论跑后补。

| 字段 | 内容 |
|------|------|
| 日期 | 2026-08-14 |
| 实验ID | EXP-18-4a (dm=64) / EXP-18-4b (dm=256) |
| 目的 | 在定案基线(sl336, pl16/s8, el=2, df=2048)上，测试 d_model 128→64/256 是否改善 |
| 改动变量 | d_model（唯一变量）：128(基线) → 64 → 256。n_heads 保持 8 |
| 对照基线 | EXP-17-0b 补测（dm=128，全步MAE=23.87, R²=0.769, E_r=-17.8） |
| 训练入口 | run_longExp.py（命令链模式） |
| features | MS |
| 口径 | 最佳 val 模型 |

---

## 可证伪假设

历史已证 dm=128 > dm=512（EXP-14 vs EXP-13）。dm=64 未测、dm=256 未测。注意 n_heads=8 固定时 d_k=dm/nh：dm=64→d_k=8（可能过小），dm=256→d_k=32。此为固定 nh 的副作用，若 dm=64/256 结果异常，Round 4b 子轮拆 n_heads 归因。

- 如果 dm=128 最优（64/256 均恶化 ≥3%）：当前宽度即甜点，维度关停
- 如果 dm=64 与基线差 <3%：选 64（参数少 4 倍）
- 如果 dm=256 改善 ≥3%：容量不足，更新基线为 256，触发 4b 子轮（n_heads=16 验证 d_k 影响）

## 预尸检

1. dm=64 + nh=8 → d_k=8，注意力头维度过小，可能明显恶化；若如此，不能归因"d_model 太小"，要区分是 d_model 还是 d_k 的锅 → 4b 子轮用 nh=4（d_k=16）验证
2. dm=256 + nh=8 → d_k=32，注意力和 FFN 参数均翻倍，训练变慢；8GB 内存安全
3. 单种子：差距在 3% 边缘时判定存疑

## 参数对照表

| 参数 | 基线(0b) | 4a | 4b | 一致？ |
|------|---------|-----|-----|--------|
| features/窗口/seq_len/pred_len | MS/W1/336/24 | 同 | 同 | ✓ |
| **d_model** | **128** | **64** | **256** | ✗ 唯一变量 |
| n_heads | 8 | 8 | 8 | ✓（副作用 d_k 变化，4b 子轮处理） |
| e_layers/d_ff | 2/2048 | 同 | 同 | ✓ |
| patch_len/stride | 16/8 | 同 | 同 | ✓ |
| revin/dropout/individual/activation | 1/0.05/0/gelu | 同 | 同 | ✓ |
| batch_size/lr/loss/epochs/patience/seed | 16/1e-4/mse/50/100/2021 | 同 | 同 | ✓ |

## 结论前提条件

features=MS、W1、最佳模型口径、50ep、单种子、sl336+pl16/s8+el=2+df=2048、n_heads=8 下成立。

## 成功标准

| 结果 | 判定 | 动作 |
|------|------|------|
| 两者均恶化 ≥3% | dm=128 甜点 | 维持，维度关停 |
| 64 差 <3% | 选 64 | 更新基线 64，关停 |
| 256 改善 ≥3% | 容量不足 | 更新基线 256 + 触发 4b 子轮 |
| 64 恶化但疑似 d_k 影响 | 存疑 | 4b 子轮用 nh=4 归因 |

---

## 结果（跑后填）

| 指标 | 基线 dm=128 | EXP-18-4a (dm=64) | EXP-18-4b (dm=256) |
|------|:---:|:---:|:---:|
| 全步 MAE | **23.87** | 24.17 | 24.70 |
| step0 | **11.39** | 13.39 | 13.67 |
| R² | **0.769** | 0.763 | 0.751 |
| E_r | -17.8 | -8.7 | **-1.4** |
| 150+ 分层 | 29.0 | 29.3 | 29.3 |
| 相对基线 | — | +1.3% | +3.5% |

## 结论（跑后填）

**✅ 通过 — 维持 d_model=128，维度关停。**

1. 回答了什么：dm=64 全步差 1.3%（无差异），dm=256 差 3.5%（恶化 ≥3%）。128 是全步/step0/R² 三项最优
2. 为何不按规则"64 差<3%→选64"：64 的 step0 (13.39) 比基线 (11.39) 恶化 17.6%——参考指标上的实质退步，不构成"无实质差异"。全步 1.3% 的差距在单种子下也无法排除真实小差异。维持 128 更稳健
3. E_r 观察（重要，留档）：dm 越大峰值幅度误差越小（128=-17.8, 64=-8.7, 256=-1.4），趋势单调。但这是以全步和 R² 恶化为代价的。若阶段 3 的预测目标高度依赖峰值幅度（26 周期最大值），dm=256 的 E_r 优势值得回看——但主指标决策下维持 128
4. 4b 子轮（n_heads）不触发：dm=256 未改善，无归因需求
5. 下一步：d_model 固定 128。Round 5（dropout）基线不变

**收尾判定**：✅ 通过 — 结论进入知识库。

## 自检

- [x] 只改 1 个变量（d_model）
- [x] 命令链模式
- [x] 物理+z-score 双报
- [x] result.txt 追加
- [x] 落盘 commit + push

---

## SOP 预检表缺项补写（2026-08-19 事后补写，只作留档）

> 本实验跑前未完整填写 SOP §1 预检表。以下内容为事后依据当时的方案讨论回填，
> 不构成'跑前已完成预检'的证明。2026-08-19 起新实验执行跑前门禁：预检表不写完不 launch。
### 文献先例（补写）

PatchTST 原文建议值与本研究基线一致；本维度（d_model/dropout/activation/individual）无太阳黑子专有文献先例，标注"本项目内首次干净对照"。
