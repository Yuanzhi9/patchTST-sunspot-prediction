# 开学讨论议程 — 一页纸（2026-08-15 版）

> 用途：开学与学姐、学长、导师的技术讨论。携带此页 + phase1_summary.md + phys_params_survey.md。
> 原则：带着数据和问题去，不让讨论变成对方单方面定方向。

---

## 我们的现状（3 句话）

1. 阶段1 完成：8 维度参数搜索，唯一实质改善 dropout（+5.2%），W1+W2 平均全步 21.33
2. 探索期 4 实验：wmse 峰值改善 92% 但全步恶化 14.6%；sqrt 滚动改善 45% 但峰值更差；RevIN 证实为滚动稳定性支柱
3. 峰值压制是系统性瓶颈（MSE/Scaler 两层机制已定位，RevIN 已排除）

## 我们的下一步（正在做）

- 变换系列（pow0.7/pow2-3/log1p/rank/boxcox）+ loss 系列（mae/阈值加权/非对称惩罚）
- block 预测策略（与滚动互备）
- 判定标准待景修批注（judgment_criteria_discussion 文档）

## 物理参数混合方案蓝图（AI 草案，待讨论）

将 Xiong 2021 四前兆参数（aa 指数/前一周峰值/本周期谷值/前一周 Skewness）+ 可选 F10.7 接入 PatchTST 的 enc_in：
- aa 指数月均序列 → 第 4 通道（1868-今，覆盖全训练段）
- 周期级慢变量 → 周期常量列
- Xiong 的时序漏洞（谷值未知）在我们滚动机制下自然规避

## 问题清单（逐人）

### 问学姐（Xiong 路线）
1. Xiong 四参数的现成数据有没有？Skewness 的具体算法？
2. 峰-峰模型（144.9）与回归（140.2）两套结果的关系？
3. 数据是 SILSO V1 还是 V2？（对齐时版本一致性）

### 问学长（M4 路线）
1. M4 的 36 个月窗口的物理依据？
2. **M4 验证流程的数据泄露核查**：拟合参数是否用到未来信息？"best_method_by_cycle"是事后选还是预先定？（我方有具体怀疑，需对质）
3. M4 的定位：它只能给平滑轮廓/峰值，不能给逐月值——两种方法任务产物不同，怎么公平对比？
4. Cycle 26 三情景（弱/中/强）的生成逻辑和置信度？

### 问导师/共同
1. 时间覆盖取舍：aa 指数 1868 起（丢 1749-1868 共 100 年）换 1 个物理参数，值不值？
2. 阶段3 的目标优先级：26 周期峰值精度 > 全程曲线 > 极小值时间？
3. 校内能否访问 ISGI/OMNIWeb 数据源？

## 讨论后要做的事

- 定物理参数接入清单（第一批：aa + Xiong 三自算参数？）
- 定阶段3 外推策略（滚动 vs block vs M4 混合）
- 定阶段2（数据更新 2026.07 重训）的最终配置
