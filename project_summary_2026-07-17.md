# PatchTST 太阳黑子预测项目 — 阶段总结报告

> 2026-07-17 | 初次科研，目的是探索"数据驱动大模型能不能做太阳黑子预测"

---

## 一、项目定位

**一句话目标：** 用 PatchTST 模型预测太阳黑子月度 SSN，核心关注第 25 太阳活动周的完整曲线预测。

- **初始动机：** 导师提到用大模型做太阳黑子预测研究，PatchTST 是 2023 年 ICLR 的通道独立 Transformer 时序预测模型，论文声称在多个基准数据集上显著优于传统方法。作为新模型 + 气象问题的结合，自然成为候选。
- **数据：** SILSO 月度太阳黑子数（SSN），1749-02 ~ 2025-10，共 3321 个月，约 24 个完整太阳活动周期。
- **身份与阶段：** 南信大气象学大二本科生，有 Python 基础但非专业编程背景。这是第一次科研尝试。
- **辅助资源：** 博士师兄已有的 M4 Waldmeier 参数化曲线预测代码（纯物理驱动方法），可作为对照基线。

**当前状态：** 15 次实验完成，最优 PatchTST 模型 R²=0.568（物理 MAE=23.87 SSN）。M4 物理方法 Cycle 25 验证 MAE=3.32，好 7 倍。Level 3 残差预测方案已验证不可行。下一步方向待定。

---

## 二、实验历程全景（6 阶段 × 15 次实验）

核心教训：第四、五阶段犯了实验方法论错误（同时改多个变量），浪费了大量实验时间。第六阶段回到单变量控制后才得到可靠结论。

### 阶段 1：代码验证

| 实验 | 内容 | 结论 |
|------|------|------|
| EXP-1 | Weather 基准数据集 | 代码环境正常，PatchTST 可运行 |

### 阶段 2：分阶段训练探索（已暂停）

三种太阳周期阶段检测方法（A v1 自适应阈值 / B 固定等分 / A v2 精修版），四阶段独立训练 + 滚动拼接预测。

- 四阶段训练结果：rise MAE=40.4 / peak MAE=51.8 / decline MAE=36.7 / trough MAE=6.3
- 滚动预测结果：MAE=64.7，峰值严重低估（预测 175 vs 真实 284）
- 发现六大工程问题（见第四节），路线暂停

### 阶段 3：全量数据 + M mode 早期尝试

| 实验 | 改动 | 结果 | 教训 |
|------|------|------|------|
| EXP-2 | Cycle 23 留出测试 | RSE=0.196 | **Z-score 指标会骗人。** RSE 好看但物理 MAE 远高于后续基线。切分方式不同，与最终基线不可比 |
| EXP-3 | 特征 4→3 列 | RSE=0.197 | year_norm 无贡献，后续固定用 3 特征 |
| EXP-4 | pred_len 96→132 | RSE=0.166~0.193 | 长 horizon 拉低平均 MSE（宁静期月份多），峰值预测依旧不准 |

### 阶段 4：MS mode 消融路线 — ★最值钱的弯路★

**起点：EXP-5 同时改三个变量**（M→MS、sl 96→132、RevIN 1→0）→ RSE 从 ~0.20 退化到 0.50+。

之后的 EXP-6~11（7 次实验）逐一排除 EarlyStopping/Scaler/激活函数/Loss，RSE 底线停在 0.389，始终回不到 M mode 的水平。

| 排除项 | 实验 | RSE | 结论 |
|--------|------|-----|------|
| EarlyStopping | EXP-6 | 0.521 | 关 ES 无改善，不是瓶颈 |
| StandardScaler→MinMax | EXP-7 | 0.397 | 有轻微改善但不根本 |
| GELU→ReLU | EXP-8 | 0.389 | 几乎一样，激活函数非关键 |
| MSE→Huber | EXP-9 | **0.389** | **全实验最重要发现：换 loss 完全不变** |
| Softplus+Huber | EXP-11 | 0.474 | 更差 |

**EXP-9 的核心发现：Huber loss 的 RSE 和 MSE 完全一样。** 换 loss 不能解决峰值压制问题——瓶颈不在损失函数形式，在预测目标本身的数值分布。直接回归 SSN 绝对值（0~300）时，峰值段大数值天然主导梯度，模型学会"保守预测以降低 training loss"。必须从任务定义层面解决。

**根本错误：** 一次改 3 变量 → 退化无法归因 → 在错误基线上做增量优化 → 7 次实验全浪费。如果当时只切 MS mode 其余不变，就能立刻判断 MS 是否可行。

### 阶段 5：1867+ 截断 + 小模型 — ★第二次方法论错误★

| 实验 | 内容 | 结果 |
|------|------|------|
| EXP-12 | 1867+ 截断 + dm128 + nh16 + el3 + df256 | RSE 0.52~0.64（4 次跑剧烈波动） |

又一次同时改多个变量（数据范围 + 4 个模型参数），不可归因。丢掉 1749-1866 约 120 年数据，训练样本减少 40%，在太阳黑子这种长期振荡问题上丢弃低频信息是方向性错误。

### 阶段 6：严格单变量对照 — 确立最优基线

| 实验 | 改动 | MSE(z) | MAE(z) | RSE | MAE 物理 | RMSE 物理 | R² |
|------|------|--------|--------|-----|----------|-----------|-----|
| EXP-13 | dm512 基线 | 0.085 | 0.141 | 0.316 | 25.27 | 34.41 | 0.539 |
| **EXP-14** ✓ | 仅 dm512→128 | **0.079** | **0.125** | **0.304** | **23.87** | **33.29** | **0.568** |
| EXP-15 | dm128 + df256 干跑 | 0.565 | 0.563 | 0.813 | — | — | — |

**全序列唯一一次严格单变量控制实验。** 降参带来泛化提升（R² +5.4%），但峰值区域（SSN > 150）误差仍 68.8 SSN。d_model 不是瓶颈，任务定义才是。

**EXP-14 误差分层（物理 MAE）：**

| SSN 区间 | MAE | 判断 |
|----------|-----|------|
| 0-50 | 6.6 | 可接受 |
| 50-100 | 12.4 | 可接受 |
| 100-150 | 27.1 | 偏高 |
| >150 | **68.8** | 严重偏低 |

---

## 三、15 次实验逻辑节点总图

```
阶段1: EXP-1 (Weather 基准) → 环境 OK ✓

阶段2: 分阶段训练 (三种周期划分 + 四模型训练) → 六大问题 → 暂停

阶段3: EXP-2 (Cycle23) → EXP-3 (特征缩简) → EXP-4 (长horizon)
        └─ 方向 OK，但切分和指标不可比

阶段4: ★弯路★ EXP-5 (一次改 3 变量) → EXP-6~11 (7次消融全浪费)
        └─ 关键发现：Huber loss = MSE loss，瓶颈在任务定义

阶段5: ★弯路★ EXP-12 (同时改数据范围 + 4 参数) → 放弃

阶段6: EXP-13 (dm512 基线) → EXP-14 (仅降参，唯一干净对照) → 确立最优
        └─ 但峰值误差 68.8 未解

── 2026-06-14 决策：走 Level 3 残差预测 ──

Level 3: M4 + PatchTST 残差预测 → 2026-07-13 确认失败
          └─ best-fit 残差≠预报残差，训练/测试分布不匹配
```

---

## 四、死路清单：哪些路已经证明走不通

以下五条路已经验证不可行。每条都标注了"为何走不通"和"一句话结论"——这是后续任何路线选择的硬约束。

### 1. 分阶段训练

| 问题 | 细节 |
|------|------|
| 为何走不通 | 每阶段 ~200 样本 vs d_model=512 约 3M 参数，严重过拟合。验证集 13 样本，EarlyStopping 不可靠。四阶段各自 fit Scaler，预测时跨阶段分布偏移。阶段路由判断不准（过度依赖 rise 模型）。误差自回归累积。 |
| 一句话总结 | 小样本 + 大模型 + 多阶段 = 三个方向的矛盾同时爆发。在当前数据规模下，"分治"不如"统合" |
| 对后续的约束 | 不要在小数据上做阶段级独立建模。如果未来一定要做多阶段，必须统一 Scaler、增加验证集、解决路由 |

### 2. MS mode

| 问题 | 细节 |
|------|------|
| 为何走不通 | MS mode 下 RSE 底线 0.389，全面劣于 M mode（RSE=0.304）。PatchTST 的通道独立设计可能与 MS mode 有结构性不匹配，加上 RevIN=0 丢掉了归一化增益 |
| 一句话总结 | MS mode + RevIN=0 组合下，无论怎么调 Scale/激活/Loss 都无法回到 M mode 水平 |
| 对后续的约束 | 固定用 M mode + RevIN=1 |

### 3. 换损失函数解决峰值压制

| 问题 | 细节 |
|------|------|
| 为何走不通 | Huber loss 的 RSE 和 MSE 完全一样（0.389）。Huber 只是对大残差惩罚从二次变线性，不改变峰值数值在梯度中的绝对主导地位 |
| 一句话总结 | 瓶颈不在 loss 形式，在预测目标的数值分布——直接回归 SSN 绝对值时，峰值段天然主导 |
| 对后续的约束 | 任何不改任务定义的局部优化（换 loss、换 optimizer、加正则项）都不会根本改善峰值预测 |

### 4. Level 3 残差预测（M4 + PatchTST）

| 问题 | 细节 |
|------|------|
| 为何走不通 | 训练用 best-fit 包络的残差（每个周期的 Gamma 曲线拟合后，残差均值为零，std=10.16，无明显预测信号）。测试时用预报残差（含 M4 对未见周期的系统性偏差）。两个分布不同，PatchTST 学到的是输出均值≈0，叠加到 M4 预报上只会加噪声（MAE 从 3.32 退化到 4.48） |
| 一句话总结 | 训练集残差（零均值拟合噪声）和测试集残差（预报偏差）不是同一种东西，模型学不到有用的修正信号 |
| 对后续的约束 | Level 3 路线不可行。如果还要做混合路线，必须从 Level 2 入手（M4 包络作为输入特征，而非预测目标） |

### 5. 截断数据 + 任意改架构

| 问题 | 细节 |
|------|------|
| 为何走不通 | 1867+ 截断丢掉 120 年低频信息，同时改 4 个参数，不可归因 |
| 一句话总结 | 太阳黑子的长期振荡（~11 年周期）需要尽量长的历史数据，截断是自残 |
| 对后续的约束 | 固定使用 1749-2025 全量数据 |

---

## 五、当前最优结果与核心矛盾

### 最优模型配置

```
Model:    PatchTST (通道独立, 纯 Encoder)
Data:     SILSO 月度 SSN, 1749-2025, 3321 月
Features: month_sin, month_cos, ssn (3 列)
Split:    train 1749-2008 (3119), val 2009-2019 (132), test 2020-2025 (70)
Params:   d_model=128, n_heads=8, e_layers=2, d_ff=2048
          seq_len=96 (8年), pred_len=24 (2年), patch_len=16, stride=8
          RevIN=1, StandardScaler, MSE loss, Adam lr=1e-4
          batch_size=16, epochs=10, seed=2021
Size:     ~245K 参数
```

### 与物理方法的横向对比

| 指标 | PatchTST (dm128) | M4 Waldmeier (Cycle 25) | 差距 |
|------|------------------|------------------------|------|
| MAE | 23.87 SSN | 3.32 SSN | **7 倍** |
| R² | 0.568 | 0.710 | -0.142 |
| 峰值误差 | -68.8 SSN | -15.9 SSN | **4 倍** |
| 数据需求 | 全量历史 (3321 月) | 仅需目标周期前 36 月 | 少 100 倍 |
| 可解释性 | 无 | 每个参数有物理含义 | — |

### 核心矛盾

PatchTST 的最优结果（R²=0.568）不算完全不能用——它在宁静期预测尚可。但它的误差结构有系统性偏差：**SSN 越高，误差越大，方向始终偏低。**

这是因为 MSE loss 下的梯度机制决定的——一个 300 SSN 样本的预测偏差 50，产生的 MSE 惩罚是 2500；一个 10 SSN 样本的偏差 30（已经误差 300%），MSE 惩罚只有 900。模型在训练中自然学会的策略是：宁可低估大值（控制大惩罚），也别冒险高估小值。这个偏向对"预测极端值"这类任务来说是致命的。

换 loss 不解决（EXP-9 已验证），降参不解决（EXP-14 已验证），换架构不解决。这是一个**任务定义层面的问题**，不是实现细节的问题。

---

## 六、太阳黑子周期物理背景

### 太阳活动周的本质

太阳内部存在一个自维持的磁流体动力学过程——**太阳发电机（solar dynamo）**：

1. **差分旋转（differential rotation）：** 太阳赤道转得比两极快 → 环向磁场被拉伸放大（ω-effect）
2. **对流层螺旋运动（cyclonic convection）：** 对流体在科里奥利力作用下扭曲环向磁场 → 产生径向磁场分量（α-effect）
3. **磁浮力：** 磁通量管因磁浮力上升至太阳表面 → 形成黑子对（leading + following spot，极性与半球和周期有关）
4. **极向场输运：** 黑子衰减后的残余磁场被表面流 + 子午环流运送到两极 → 反向 → 进入下一个半周期

整个过程约需 11 年（实际为 22 年的完整磁周期），但在细节上高度不规则——每个周期的升速、峰值强度、持续时间和形态都不同。

### Waldmeier 效应（1939）

Waldmeier 发现：一个太阳活动周的**上升速度**和**峰值强度**之间存在正相关——上升越快的周期，峰值越高。这个经验关系至今没有完全令人满意的物理解释，但它是所有基于早期观测做预测的方法（包括 M4）的核心先验。

### 为什么这个问题很难

1. **太阳内部不可观测。** 发电机过程发生在对流层底部和辐射区交界处，我们没有任何直接测量，能看到的只有表面黑子
2. **样本极少。** 可靠的黑子望远镜记录从 1749 年开始，只有约 24 个完整周期。从统计学习角度看，这是极小的样本
3. **高度非线性。** 发电机方程是耦合的非线性偏微分方程组，周期形态对初始条件和参数极其敏感
4. **驱动因素复杂。** 除了太阳自身发电机，还有行星潮汐力（争议）、太阳内部湍流随机涨落等外部和内部因素

### 有什么影响

这些物理特征直接决定了建模的约束条件：

| 物理特征 | 对建模的意味 |
|----------|-------------|
| 只有 24 个周期 | 任何纯数据驱动方法都面临极端的样本不足 |
| 周期形态各异 | 历史周期之间的变异性很大，training 和 test 可能来自不同分布 |
| Waldmeier 效应存在 | 早期观测的信息量很集中——上升期的形态和速度携带了峰值的关键信息 |
| 长期记忆 + 周期非平稳 | 需要很长的输入窗口，但窗口内分布可能已经偏移 |

### 推荐文献

- **Hathaway (2015) "The Solar Cycle", Living Reviews in Solar Physics.** 100+ 页的经典综述，覆盖黑子观测历史、周期统计特征、发电机理论、预测方法。读前半（观测统计部分）就够了，发电机理论第二部分可以跳读。**RSSN 指数变化、Waldmeier 效应、Gnevyshev gap 等核心概念都在这里。**
- **Petrovay (2020) "Solar cycle prediction", Living Reviews in Solar Physics.** 专门讲预测方法的综述，覆盖从曲线拟合到机器学习到发电机模型的全谱系。能帮你理解"你的竞争对手们在用什么方法、做到什么精度了"。

---

## 七、实验方法论问题自检

### 数据层面

1. **样本总量小，评估子集更小。** 全量 3321 月，但 val 只有 132 个月（11 年）、test 只有 70 个月（~6 年）。test 集仅覆盖 Cycle 25 的前半段，远不够代表一个完整周期的多样性。在这个 70 个月窗口上跑出 R²=0.568，但没有做任何形式的交叉验证或多窗口评估来确认稳定性。

2. **数据划分有泄漏风险。** 当前 `Dataset_Custom` 类硬编码了 train/val/test 的边界（`num_test=70, num_val=132`），但没有检查 seq_len=96 的滑动窗口是否跨越边界。在时间序列中，如果 train 的最后一个样本包含 val 的时间段（因为 96 个月窗口跨越了 2008-12 和 2009-01 的交界），则有数据泄漏。

3. **单次运行无误差估计。** 所有实验只跑了 1 次（seed=2021 固定），没有报告置信区间或标准差。对于 R²=0.568 这个结果，无法判断是稳定表现还是运气好的一次。

4. **测试集选择巧合。** Cycle 25 的峰值约在 2024-07 达到 159 SSN，而测试集刚好从 2020-01 到 2025-10，包含了完整的上升段和峰值区。如果测试集选取的是宁静期，MAE 可能好看很多。这意味着当前 23.87 的 MAE 已经是"在最难的时间段上测出来的"——这既是诚实，也意味着模型在其他时间段上的表现可能被低估了。

### 实验层面

1. **实验方法论自觉不足。** 第四、五阶段连续两次犯"同时改多个变量"的错误，浪费了 8 次实验。这不是技术问题，是实验设计意识问题。后续任何实验必须遵循：**一次只改一个变量，否则不可归因。**

2. **评估指标过于单一。** 目前主要看 MSE/MAE/RSE。缺少：
   - 峰值相关指标（峰值时间偏差、峰值强度偏差）
   - 周期形态指标（上升段/下降段各段的 MAE）
   - 预测不确定度（ensemble 或 MC dropout 的概率分布）

3. **缺少消融实验的正反设计。** 消融的合理做法是：先确立基线 → 逐一去掉组件 → 看哪个组件对性能影响最大。当前只有 d_model 的对照是合格的消融。

### 建议补充

- Blocked time series cross-validation（留出一个完整周期或一个十年做测试，滚动多次）
- 多次随机种子取平均 ± 标准差
- 和简单基线对比（如持久性预测、ARIMA、简单 LSTM）

---

## 八、文献需求清单

### A. 太阳物理（理解问题本质）— 优先

1. **Hathaway (2015) "The Solar Cycle", Living Reviews in Solar Physics.**
   - 经典综述。先读前半部（§1-4：观测统计、周期特征），发电机理论（§5-6）可以跳读
   - 重点：Waldmeier 效应、Gnevyshev gap、周期不对称性、长期调制特征
   
2. **Petrovay (2020) "Solar cycle prediction", Living Reviews in Solar Physics.**
   - 预测方法综述。从 precursor methods 到 dynamo models 到 machine learning
   - 重点：了解"别人都用了什么方法，精度到什么水平"，避免用 DL 去打已经知道会输的仗

### B. 时间序列深度学习 — 重点

1. **Nie, Y. et al. (2023) "A Time Series is Worth 64 Words: Long-term Forecasting with Transformers", ICLR 2023.**
   - PatchTST 原文。你已经实现了，但可能需要重读：重点看 Patching 的动机（为什么不是逐点？）、通道独立设计（为什么不是混合通道？）、和其他 Transformer 变体的对比
   
2. **对比模型论文（按需浏览）：**
   - TimesNet (Wu et al., 2023 ICLR) — 二维视角处理时序，对周期性数据可能有优势
   - DLinear (Zeng et al., 2023 AAAI) — 只是一个线性层 + 反归一化，在很多数据集上击败了 Transformer。这个值得你认真想想：如果 DLinear 一个线性层就能比 PatchTST 好，那说明你数据的预测信号可能很浅，不需要 Transformer
   - iTransformer (Liu et al., 2024 ICLR) — 在变量维度上做 attention

### C. 实验方法论

1. **Bergmeir & Benítez (2012) "On the use of cross-validation for time series predictor evaluation", Information Sciences.**
   - 时间序列上的交叉验证该怎么做。不是随机 shuffle，而是 blocked CV
   
2. **Roberts et al. (2017) "Cross-validation strategies for data with temporal, spatial, hierarchical, or phylogenetic structure", Ecography.**
   - 带结构数据的 CV 策略。比你需要的更宽泛，但方法论是一致的

### D. 太阳黑子预测 + 机器学习的已有工作（搜索关键词）

在 Google Scholar / ADS 上搜：
- `deep learning solar cycle prediction`
- `transformer sunspot prediction`
- `neural network solar activity forecast`
- `machine learning SSN`

重点找 2020 年后的工作，看他们的实验设计怎么做的。

---

## 九、向老师提问的问题清单

按优先级排序。第一组是方向性问题（必须先搞清楚），第二组是技术性问题（可以先和师兄讨论）。

### 方向组

1. **大模型预测太阳黑子，核心价值在哪里？** 如果物理方法（M4）只用 36 个月观测就能做到 MAE=3.32，而 PatchTST 用 300 年的数据才 MAE=23.87——那研究这条路的合理性在哪？是方法上的创新（提出一种新的混合预测框架），还是应用上的验证（证明 Transformer 在这个问题上有效/无效），还是教学性探索（本科生练习科研过程）？

2. **期望的产出形式是什么？** 是写一篇论文（投哪里？）、一份技术报告、还是一篇本科毕业论文？不同形式的深度要求不同，会影响接下来的工作量。

3. **这个方向在您的课题组是什么定位？** 是独立的探索性课题，还是为师兄师姐的更大项目提供技术验证？有没有可能和组内其他同学的工作形成互补？

4. **对"做不出来"的容忍度？** 考虑到只有 3321 个月数据 + 24 个周期，深度学习在这个问题上的上限可能天然很低。如果最终结论是"深度学习在纯数据驱动模式下不适合太阳黑子预测"，这算不算一个合格的结论？

### 技术组

5. **是否应该引入更多物理量？** 目前只用 SSN。是否考虑加入 F10.7 射电通量（反映太阳极紫外辐射水平）、地磁 aa 指数（反映太阳风-磁层耦合）、或其他空间天气指标？引入多变量可能帮助模型学到更丰富的特征，但也带来更多噪声。老师在气象领域对"多变量 vs 单变量"有什么经验？

6. **评估策略是否合理？** 当前的 train/val/test = 3119/132/70 月，val 和 test 非常小。有没有更标准的评估方法（如 leave-one-cycle-out）？

---

## 十、向师兄提问的问题清单

1. **M4 四个参数（A, tp, alpha, floor），哪个对预测最敏感？** 如果拟合中某个参数的不确定度很大（比如 alpha 的变动对曲线形态影响有限），那也许 PatchTST 修正的重点就不在那个参数上。反过来，如果峰值振幅 A 是最核心但最难确定的，那 PatchTST 也许应该专门修正 A。

2. **M4 在 Cycle 25 上低估了 15.9 SSN，这个误差是从哪里来的？** 是 Waldmeier 先验本身偏保守？是 36 个月观测段还没完全覆盖上升期特征？还是 Gamma 曲线函数形式本身有局限（比如不能描述双峰结构）？

3. **为什么恰好是 36 个月？** 你用 36 个月早期观测就能做到 Cycle 25 MAE=3.32，这个时间长度是经验调试出来的，还是有物理依据（比如 Waldmeier 效应的信息在大约 2-3 年的上升段后就能稳定提取）？如果用 24 个月或 48 个月，性能会退化多少？

4. **你怎么看"M4 包络作为 PatchTST 输入特征"这个思路？** Level 3 残差预测已经失败（训练残差和预报残差不是同一种东西），退回到 Level 2（M4 包络作为输入特征，预测目标仍是 SSN）的话，你觉得可行吗？有没有你从 M4 角度看到的潜在坑？

5. **你做 M4 的时候遇到过什么数据问题？** 比如最小期/极大期的标注（不同的官方标定可能有几个月的差异）、历史 SSN 数据的版本差异（SILSO 2.0 和旧版的区别）、周期划分的边界选取等。有没有什么注意事项？

---

## 十一、方向建议与下一步

以下四个方向不是互斥的，可以并行推进。

### 方向 A：Level 2 混合路线 — M4 包络作为 PatchTST 输入特征

**做什么：** enc_in 从 3 变为 4（month_sin, month_cos, ssn, M4_envelope），预测目标仍然是 SSN。M4 包络作为"物理先验特征"输入，给模型一个参考基线。

**为什么值得试：** Level 3 残差预测的失败点在于"训练集残差和测试集残差不同分布"。但 Level 2 不改变预测目标——它只是把 M4 的信息作为额外输入通道，模型自己决定用不用、怎么用。风险比 Level 3 小很多。

**风险：** 模型可能只学会"copy M4 包络 + 轻微扰动"，而不是真正用 SSN 信号做预测。如果最终结果和纯 M4 差不多，那就回归到一个尴尬的结论。

**建议：** 先做一个快速实验验证可行性。如果 Level 2 效果接近于纯 M4 但不如 M4+简单修正，就考虑放弃深度学习路线，承认结论。

### 方向 B：从预测任务转为"不确定性估计"任务

**做什么：** 不追求预测具体的 SSN 值，而是给出"给定过去 8 年，未来 2 年 SSN 的概率分布"。

**好处：** 这避开了"MSE loss 压制峰值"的死结——输出概率分布比输出单点值更容易处理极端值。而且在实际应用中（比如卫星轨道规划、电网规划），"SSN 有 90% 概率不会超过 200"比"预测 SSN=183"更有用。

**方法：** MC Dropout（训练好的模型在推理时 keep dropout on，多次采样取分布），或直接用 Gaussian Mixture 输出头。

### 方向 C：引入更多外部数据源

**做什么：** 除 SSN 外，加入 F10.7 射电通量（1947 年起）、地磁 aa 指数（1868 年起）、太阳总辐照度 TSI 等。增大特征维度和信息量。

**局限：** 这些数据的时间覆盖不如 SSN（很多只有 1950 年代后），而且可能高度共线。但从物理角度看，太阳活动是一个多波段多形态的系统性现象，单看 SSN 可能丢失了大量信息。

### 方向 D：坦诚定位——"深度学习在太阳黑子预测中的可行性探索"

**做什么：** 不继续追求提升指标，而是把已经有的实验整理成一个完整的"探索报告"。

**故事的逻辑线：**
1. 太阳黑子周期是强物理约束的问题，传统物理方法已经很有效（M4 MAE=3.32）
2. 纯数据驱动方法（PatchTST）在这个极端小样本、强周期性、数值范围变化大的问题上暴露了系统性局限
3. 通过 15 次系统实验和 5 条死路，归纳出了深度学习方法在这个问题上的关键矛盾
4. 提出融合物理先验的混合建模方向（如果做了方向 A 的话）

**这个定位的好处：** 它不是"做不出来所以放弃"，而是"通过系统实验确认了深度学习的边界，并在此基础上提出融合路线"。这在学术上是合理的——失败实验的结论和成功实验的结论一样有价值，只要分析到位。

### 建议组合

```
短期（1-2 周）:
├── 读 Hathaway (2015) 综述
├── 跑 Level 2 快速实验（enc_in=4，加入 M4 包络特征）
└── 找 3 篇"深度学习 + 太阳黑子"的论文，看清楚别人怎么做的

中期（1-2 月）:
├── 如果 Level 2 有效：深化实验，加入更多评估维度
├── 如果 Level 2 无效：转为方向 D，整理成探索报告
└── 补齐实验方法论短板（CV、多次 seed、物理单位误差分层）

带去向老师讨论:
└── 带着 Level 2 的结果 + 这份总结报告 + 你的方向判断去见老师
```

---

## 附录：项目文件索引

### 关键文件

| 文件 | 用途 |
|------|------|
| `AGENTS.md` | 项目规范、参数约定、实验结论 |
| `PatchTST_supervised/report_2026-06-14.md` | 上次详细汇报（465 行） |
| `result.txt` | 全部 15 次实验指标 |
| `diary/2026-05-27.md` | 分阶段训练日记 |

### 训练入口

| 文件 | 说明 |
|------|------|
| `run_sunspot_fixed.py` | 主训练入口 (d_model=128, epochs=10) |
| `PatchTST_supervised/run_sunspot_fixed.py` | 快速测试 (epochs=1) |

### 核心代码

| 文件 | 职责 |
|------|------|
| `PatchTST_supervised/layers/PatchTST_backbone.py` | 核心架构 (Patching + Transformer + Flatten Head) |
| `PatchTST_supervised/models/PatchTST.py` | 模型 wrapper |
| `PatchTST_supervised/exp/exp_main.py` | 训练/验证/测试循环 |
| `PatchTST_supervised/data_provider/data_loader.py` | Dataset_Custom (数据加载 + 切分) |

### Level 3 分支（`level3-residual-prediction` worktree）

| 文件 | 用途 |
|------|------|
| `PatchTST_supervised/prepare_level3_residual.py` | M4 校准 + 预报 + 残差生成 |
| `PatchTST_supervised/eval_level3_residual.py` | 残差预测评估 |
| `run_sunspot_residual_fullfit.py` | 残差训练入口 |

### 师兄 M4 代码

| 文件 | 用途 |
|------|------|
| `PatchTST_supervised/shixiong_m4/coding/.../solar_cycle_optimization_framework.py` | M4 框架 (917 行) |
| `.../cycle24_cycle25_all_validation_forecasts.csv` | Cycle 24/25 逐月预测 |
| `.../cycle26_weak_medium_strong_scenarios.csv` | Cycle 26 三情景 |

### 数据

| 文件 | 说明 |
|------|------|
| `PatchTST_supervised/dataset/sunspot_with_cycle.csv` | 主数据 (3321 行) |
| `PatchTST_supervised/dataset/sunspot_1867-02_2025-10_original_sincos.csv` | 1867+ 截断版 (1905 行) |
